# Base URL of the API. Leave blank in development to use the Vite proxy (/api).
VITE_API_URL=

# Only used by `npm run dev` to point the proxy at a non-default API port.
VITE_PROXY_TARGET=http://localhost:5000
